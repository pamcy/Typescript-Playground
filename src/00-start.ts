// Note
// run 'npx tsc -w' in the terminal

// if you want to
// disable emitting files if any type checking errors are reported. (暫時性的需求：有錯誤就不要編譯成 js)
// run 'npx tsc --noEmitOnError -w'

let username = 'Pamcy'
console.log(username);

let a = 12
let b = '6'
let c = 2

console.log(a / b);
